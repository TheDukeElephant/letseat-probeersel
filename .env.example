# Root example environment variables (add per app specifics in their folders)
# Copy to .env and adjust as needed.

# Global
NODE_ENV=development

# Backend
DATABASE_URL=postgresql://letseat:prisma@localhost:5434/prisma?schema=public
PORT=4000
JWT_SECRET=change_me_to_a_32+_char_random_string
